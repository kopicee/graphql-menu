#!/usr/bin/env sh

if ! gem list foreman -i --silent; then gem install foreman; fi
if ! gem list rerun -i --silent; then gem install rerun; fi

export RUBY_DEBUG_OPEN="true"
export RUBY_DEBUG_LAZY="true"

exec foreman start -f Procfile.dev "$@"
